DotF Neovim:
  ☐ Make box comment filetype aware and map to `cc` @priority(high)
  ☐ Fix DotF bootstrap sync of modules @priority(high)
  ☐ Map NERDCommenter commands
  ☐ Change devicons colors to match gruvbox theme @priority(low)
  ☐ Check if file has changed and reload tasks @priority(low)
  ☐ Make task file loading generic in searching for *.todo @priority(low)
