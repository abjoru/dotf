# If a config.py file exists, this file is ignored unless it's explicitly loaded
# via config.load_autoconfig(). For more information, see:
# https://github.com/qutebrowser/qutebrowser/blob/master/doc/help/configuring.asciidoc#loading-autoconfigyml
# DO NOT edit this file by hand, qutebrowser will overwrite it.
# Instead, create a config.py - see :help for details.

config_version: 2
settings:
  aliases:
    global:
      q: quit
      w: session-save
      wq: quit --save
  bindings.commands:
    global:
      normal:
        xb: config-cycle statusbar.show always in-mode
        xt: config-cycle tabs.show always switching
        xx: config-cycle statusbar.show always in-mode ;; config-cycle tabs.show always
          switching
        zl: spawn --userscript qute-pass
        zpl: spawn --userscript qute-pass --password-only
        zul: spawn --userscript qute-pass --username-only
  content.notifications:
    https://www.youtube.com: true
  downloads.location.directory:
    global: ~/Downloads
  statusbar.show:
    global: in-mode
  tabs.show:
    global: always
  url.searchengines:
    global:
      DEFAULT: https://www.google.com/search?hl=en&q={}
      aw: https://wiki.archlinux.org/?search={}
      yt: https://www.youtube.com/results?search_query={}
  url.start_pages:
    global: ~/.cache/dotf/homepage.html
